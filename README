HOW TO COMPILE
Please use `git clone --recurse-submodules <url>` to install properly - otherwise submodules will not be downloaded
Once downloaded, run `make` in the root project folder. The final image will written to build/disk.img

HOW TO USE
The image can be written to a flash drive directly using `dd`, or by using a tool such as balenaEtcher